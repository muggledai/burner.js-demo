/** React **/
import React from 'react';
import { render } from 'react-dom';

/** Definitions **/
const App = ({}) => {
    return (
        <div>burner.js</div>
    );
}
/** Render to Page **/
render(<App />, document.getElementById('app'));
